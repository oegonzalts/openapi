properties:
  name:
    type: string
  affiliateId:
    type: string
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
  status:
    $ref: ./ClientStatus.yaml
  isArchived:
    type: boolean
  publicKey:
    type: string
  regenKey:
    type: string
required:
  - name
  - affiliateId
  - createdAt
  - updatedAt
  - status
  - isArchived
  - publicKey
  - regenKey
type: object
description: 'From T, pick a set of properties whose keys are in the union K'
