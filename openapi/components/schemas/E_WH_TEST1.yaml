properties:
  message:
    type: string
    enum:
      - >-
        Subscription does not exist or is not assigned to corresponding
        campaign.
    nullable: false
  code:
    type: string
    enum:
      - E_WH_TEST
    nullable: false
required:
  - message
  - code
type: object
