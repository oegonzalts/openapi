properties:
  name:
    type: string
  parentId:
    type: string
  createdAt:
    type: string
    format: date-time
  updatedAt:
    type: string
    format: date-time
  status:
    $ref: ./AffiliateStatus.yaml
  isArchived:
    type: boolean
required:
  - name
  - parentId
  - createdAt
  - updatedAt
  - status
  - isArchived
type: object
description: 'From T, pick a set of properties whose keys are in the union K'
